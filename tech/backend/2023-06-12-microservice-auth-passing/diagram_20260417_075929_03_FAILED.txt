# Failed image prompt — block 3
# Diagram type: sequencediagram
# Source line: 368

## Original Mermaid Source

```mermaid
sequenceDiagram
    participant C as 客户端
    participant G as API 网关
    participant Auth as 认证服务

    C->>G: 1. 请求资源（Access Token 已过期）
    G-->>C: 2. 返回 401 Unauthorized

    C->>Auth: 3. POST /auth/refresh {refreshToken}
    Auth->>Auth: 4. 验证 Refresh Token
    Auth->>Auth: 5. 生成新的 Access Token
    Auth-->>C: 6. 返回新的 {accessToken}

    C->>G: 7. 使用新 Token 重新请求
    G-->>C: 8. 返回业务数据
```

## Image Prompt

Chiikawa official manga style educational illustration, 4:3, explaining API authentication token refresh flow.

=== TITLE ===
"Token 刷新流程" — floating typography, NOT anchored to any solid band or box; coral-pink (#FF8E9E) bold rounded font with White Outer Glow and Subtle Drop Shadow for legibility on light backgrounds; surrounded by small pastel flowers, twinkling stars, and tiny hearts woven naturally into the scene

=== SCENE & LAYOUT ===
Layout: Vertical flow with connected steps, each step showing interaction between different actors.

Background: Soft gradient cream (#FFF8E7) to pastel lavender (#E6E6FA), minimal scattered sparkles

=== MAIN CONTENT ===
Use official Chiikawa characters such as Chiikawa / Hachiware / Usagi with clearly different appearances and roles
[CONTENT_SECTION_1]
Character: Chiikawa (吉伊)
- Chiikawa, with her round white body and pink blush, looks concerned while holding a phone, a speech bubble above her.
- Mobile phone with a request icon
Visual: Client-side request to API gateway.
Label: "请求资源 (Request Resource)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "我的 Token 过期了吗？(Is my token expired?)"

[CONTENT_SECTION_2]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, looks stern, holding a clipboard, presenting a rejection notice.
- Clipboard with "401 Unauthorized" written on it.
Visual: API Gateway returns 401 error to the Client.
Label: "返回 401 (Return 401)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "未授权！请刷新 Token。(Unauthorized! Please refresh the token.)"

[CONTENT_SECTION_3]
Character: Chiikawa (吉伊)
- Chiikawa, with her round white body and pink blush, looks determined, sending a refresh token.
- Small envelope labeled "refreshToken".
Visual: Client sends refresh token to the Authentication Service.
Label: "请求刷新 (Request Refresh)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "请求刷新 Token！(Requesting refresh token!)"

[CONTENT_SECTION_4]
Character: Usagi (乌萨奇)
- Usagi, the energetic beige rabbit, is checking a list with a magnifying glass.
- Magnifying glass and a checklist.
Visual: Authentication Service verifying the refresh token.
Label: "验证 Token (Verify Token)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "Kua! 验证！(Kua! Verifying!)"

[CONTENT_SECTION_5]
Character: Usagi (乌萨奇)
- Usagi, the energetic beige rabbit, operates a complex token generator machine.
- Machine with gears and lights producing a token.
Visual: Authentication Service generating a new Access Token.
Label: "生成 Token (Generate Token)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "咻咻！新 Token！(Shush Shush! New Token!)"

[CONTENT_SECTION_6]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, hands over a new token with a smile.
- Token with a ribbon.
Visual: Authentication Service returns new Access Token to Client.
Label: "返回 Token (Return Token)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "这是新的 Token！(Here's the new token!)"

[CONTENT_SECTION_7]
Character: Chiikawa (吉伊)
- Chiikawa, with her round white body and pink blush, happily resends the request with the new token.
- Request with the new token.
Visual: Client resends request to API Gateway with new Token.
Label: "重新请求 (Resend Request)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "用新 Token 重新请求！(Resending with the new token!)"

[CONTENT_SECTION_8]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, happily returns the requested data.
- Data package with a smile.
Visual: API Gateway returns business data.
Label: "返回数据 (Return Data)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "数据返回！(Data returned!)"

=== KEY CONCEPTS ===
4 floating info boxes; each box uses a semi-transparent cloud-like blob background (marshmallow-soft, pure white at 70–80% opacity, no hard border), text in dark charcoal (#2D2D2D), medium-weight sans-serif:
Box 1: "Access Token": 用于访问受保护资源的凭证 (Credentials for accessing protected resources)
Box 2: "Refresh Token": 用于获取新 Access Token 的凭证 (Credentials for obtaining a new Access Token)
Box 3: "401 Unauthorized": 表示未授权的 HTTP 状态码 (HTTP status code indicating unauthorized access)
Box 4: "API 网关 (API Gateway)": 系统的入口点 (The entry point of the system)

=== CHIIKAWA STYLE ===
Official Chiikawa aesthetic: round bodies, large sparkly eyes, pink blush marks, tiny limbs, super deformed proportions, gentle line art

=== TEXT RENDERING ===
Typography rules (CRITICAL — apply to every label, title, and annotation):
- Title: floating typography with White Outer Glow or Subtle Drop Shadow — NO solid background band; title integrates naturally into the illustrated scene with decorative Chiikawa elements around it
- All labels placed inside semi-transparent cloud-like blobs (marshmallow-soft, pure white at 70–80% opacity, no hard borders) — soft edges naturally blend into the illustrated background
- Font: bold (600–700) rounded sans-serif for title; medium-weight (500–600) for labels; NO thin, cursive, or brush-stroke fonts
- Text color: dark charcoal (#2D2D2D) on the light blob background; minimum contrast 4.5:1
- Chinese characters: every stroke and radical must be Fully Formed and Vector-style clarity — no hairline breaks, no missing components, no pixelation
- Smooth anti-aliased edges; absolutely NO cracked, fragmented, or glitchy strokes

=== MOOD ===
Informative, friendly, cute but educational

Quality: High-quality Chiikawa illustration, clear visual hierarchy, for educational purposes
