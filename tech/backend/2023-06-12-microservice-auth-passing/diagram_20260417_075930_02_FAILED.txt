# Failed image prompt — block 2
# Diagram type: sequencediagram
# Source line: 168

## Original Mermaid Source

```mermaid
sequenceDiagram
    participant C as 客户端
    participant G as API 网关
    participant Auth as 认证服务
    participant Biz as 业务服务

    C->>Auth: 1. POST /auth/login {username, password}
    Auth->>Auth: 2. 验证凭证，生成 JWT
    Auth-->>C: 3. 返回 {accessToken, refreshToken}
    Note over C: 客户端存储令牌

    C->>G: 4. GET /api/orders (Authorization: Bearer <JWT>)
    G->>G: 5. 解析并验证 JWT 签名
    G->>G: 6. 检查 JWT 是否过期
    G->>Biz: 7. 转发请求 + 用户上下文
    Biz->>Biz: 8. 检查用户权限
    Biz-->>C: 9. 返回业务数据
```

## Image Prompt

Chiikawa official manga style educational illustration, 16:9, explaining API Authentication Flow.

=== TITLE ===
"API 身份验证流程 (API Authentication Flow)" — floating typography, NOT anchored to any solid band or box; coral-pink (#FF8E9E) bold rounded font with White Outer Glow and Subtle Drop Shadow for legibility on light backgrounds; surrounded by small pastel flowers, twinkling stars, and tiny hearts woven naturally into the scene

=== SCENE & LAYOUT ===
Layout: Winding roadmap journey illustrating a multi-step process, starting on the left and winding towards the right; each step represented by a distinct scene

Background: Soft gradient cream (#FFF8E7) to pastel sky-blue (#D0E7FF), minimal scattered clouds

=== MAIN CONTENT ===
Use official Chiikawa characters such as Chiikawa / Hachiware / Usagi with clearly different appearances and roles
[CONTENT_SECTION_1]
Character: Chiikawa (吉伊)
- Chiikawa, the small white marshmallow, is nervously typing on a tiny laptop with a determined expression.
- Laptop showing a login form with username and password fields.
Visual: A miniature cityscape representing the client-side application.
Label: "1. POST /auth/login" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "用户名…密码… (Username… Password…)"

[CONTENT_SECTION_2]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, sits at a desk with a computer monitor displaying code, looking thoughtful as he reviews credentials and generates a JWT.
- Clipboard with "JWT" written on it.
Visual: A stylized server room with blinking lights, representing the authentication service.
Label: "2. 验证凭证，生成 JWT (Verify credentials, generate JWT)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "凭证验证通过…JWT 生成！(Credentials verified… JWT generated!)"

[CONTENT_SECTION_3]
Character: Chiikawa (吉伊)
- Chiikawa, the small white marshmallow, is happily receiving a token and storing it safely.
- Chiikawa is holding a small scroll labeled "accessToken, refreshToken."
Visual: A treasure chest with a lock, symbolizing secure storage.
Label: "3. 返回 {accessToken, refreshToken}" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "安全存储！(Stored securely!)"
Annotation: 客户端存储令牌 (Client stores the token)

[CONTENT_SECTION_4]
Character: Chiikawa (吉伊)
- Chiikawa, the small white marshmallow, is sending an API request.
- Holding a phone and sending a message bubble with data.
Visual: A miniature cityscape representing the client-side application.
Label: "4. GET /api/orders" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "发送订单请求！(Sending order request!)"
Annotation: Authorization: Bearer <JWT>

[CONTENT_SECTION_5]
Character: Usagi (乌萨奇)
- Usagi, the tall beige rabbit, intensely focused, examining a complex equation.
- Holding a magnifying glass and pointer.
Visual: A complex network diagram representing the API Gateway.
Label: "5. 解析并验证 JWT 签名 (Parse and verify JWT signature)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "验证！验证！(Verify! Verify!)"

[CONTENT_SECTION_6]
Character: Usagi (乌萨奇)
- Usagi, the tall beige rabbit, frantically checking a giant clock.
- Waving his arms.
Visual: A large clock with a timer.
Label: "6. 检查 JWT 是否过期 (Check if JWT is expired)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "时间！时间！(Time! Time!)"

[CONTENT_SECTION_7]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, forwarding a request with user context.
- Holding a clipboard with user info and passing it forward.
Visual: A tunnel connecting the API Gateway to the Business Service.
Label: "7. 转发请求 + 用户上下文 (Forward request + User context)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "用户上下文已添加！(User context added!)"

[CONTENT_SECTION_8]
Character: Hachiware (小八)
- Hachiware, the blue-marked cat, checking a list of permissions.
- Cross-referencing permissions against the user.
Visual: A database server with access control lists.
Label: "8. 检查用户权限 (Check User Permissions)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "权限检查…通过！(Permissions check… Passed!)"

[CONTENT_SECTION_9]
Character: Chiikawa (吉伊)
- Chiikawa, the small white marshmallow, happily receiving business data.
- Holding a document scroll with a happy face.
Visual: A completed order form.
Label: "9. 返回业务数据 (Return Business Data)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, no hard border, soft edges blending into the background)
Dialogue: "订单完成！(Order complete!)"

=== KEY CONCEPTS ===
5 floating info boxes; each box uses a semi-transparent cloud-like blob background (marshmallow-soft, pure white at 70–80% opacity, no hard border), text in dark charcoal (#2D2D2D), medium-weight sans-serif:
Box 1: "JWT (JSON Web Token)"
Box 2: "accessToken (访问令牌)"
Box 3: "refreshToken (刷新令牌)"
Box 4: "API 网关 (API Gateway)"
Box 5: "身份验证 (Authentication)"

=== CHIIKAWA STYLE ===
Official Chiikawa aesthetic: round bodies, large sparkly eyes, pink blush marks, tiny limbs, super deformed proportions, gentle line art

=== TEXT RENDERING ===
Typography rules (CRITICAL — apply to every label, title, and annotation):
- Title: floating typography with White Outer Glow or Subtle Drop Shadow — NO solid background band; title integrates naturally into the illustrated scene with decorative Chiikawa elements around it
- All labels placed inside semi-transparent cloud-like blobs (marshmallow-soft, pure white at 70–80% opacity, no hard borders) — soft edges naturally blend into the illustrated background
- Font: bold (600–700) rounded sans-serif for title; medium-weight (500–600) for labels; NO thin, cursive, or brush-stroke fonts
- Text color: dark charcoal (#2D2D2D) on the light blob background; minimum contrast 4.5:1
- Chinese characters: every stroke and radical must be Fully Formed and Vector-style clarity — no hairline breaks, no missing components, no pixelation
- Smooth anti-aliased edges; absolutely NO cracked, fragmented, or glitchy strokes

=== MOOD ===
Informative, friendly, cute but informative

Quality: High-quality Chiikawa illustration, clear visual hierarchy, for documentation
