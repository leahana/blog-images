Chiikawa official manga style educational illustration, 4:3, explaining Redis serialization strategies.

=== TITLE ===
"Redis 序列化方案选择" — floating typography, NOT anchored to any solid band or box; coral-pink (#FF8E9E) bold rounded font with White Outer Glow and Subtle Drop Shadow for legibility on light backgrounds; surrounded by small pastel flowers, twinkling stars, and tiny hearts woven naturally into the scene

=== SCENE & LAYOUT ===
Layout: Decision tree with branching paths showing two different Redis serialization approaches, connected by arrows.

Background: Soft gradient cream (#FFF8E7) to pastel lavender (#E6E6FA), minimal scattered stars and musical notes

=== MAIN CONTENT ===
Use official Chiikawa characters such as Chiikawa / Hachiware / Usagi with clearly different appearances and roles
[CONTENT_SECTION_1]
Character: Hachiware (小八)
- Hachiware, the blue-eared cat, stands thoughtfully with a clipboard in paw. He's reviewing a document with a magnifying glass.
- Clipboard with checklist, magnifying glass
Visual: A large flowchart decision diamond containing text.
Label: "业务层是否已用 ObjectMapper 序列化? (Has the business layer been serialized using ObjectMapper?)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "让咱仔细看看…序列化的情况如何呢？(Let me take a closer look... what's the serialization status?)"

[CONTENT_SECTION_2]
Character: Chiikawa (吉伊)
- Chiikawa, the small white character with dot eyes, holds a "YES" sign. He's pointing towards a Redis server rack illustration.
- "YES" sign, pointing hand, miniature Redis server rack
Visual: A simplified representation of Redis server rack.
Label: "使用 StringRedisSerializer (Use StringRedisSerializer)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "是！字符串万岁！(Yes! Long live strings!)"

[CONTENT_SECTION_3]
Character: Usagi (乌萨奇)
- Usagi, the energetic rabbit, holds a "NO" sign while jumping excitedly.
- "NO" sign, jumping pose, energetic expression
Visual: A jar of JSON data represented with curly braces and key-value pairs
Label: "使用 Jackson2JsonRedisSerializer (Use Jackson2JsonRedisSerializer)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "喔——！直接上 JSON！(Ooooh! Let's go with JSON directly!)"

[CONTENT_SECTION_4]
Character: Chiikawa (吉伊)
- Chiikawa, holding a small package labeled "String Data", is carefully placing it into a Redis box.
- Package labeled "String Data", Redis box
Visual: A conveyor belt transporting string data into a Redis database.
Label: "RedisTemplate 只做字符串搬运 (RedisTemplate only handles string transportation)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "我只是个搬运工…(I'm just a transporter...)"

[CONTENT_SECTION_5]
Character: Hachiware (小八)
- Hachiware, with his blue ears and clipboard, watches as JSON objects automatically serialize and store themselves in Redis.
- Clipboard with checkmark, watching attentively
Visual: JSON objects automatically transforming and entering a Redis database.
Label: "RedisTemplate 自动处理序列化 (RedisTemplate automatically handles serialization)" — inside a semi-transparent cloud-like blob (marshmallow-soft aesthetic, pure white at 70–80% opacity, NO hard border, soft edges blending into the background)
Dialogue: "Redis 帮我们搞定啦！(Redis takes care of it for us!)"

=== KEY CONCEPTS ===
5 floating info boxes; each box uses a semi-transparent cloud-like blob background (marshmallow-soft, pure white at 70–80% opacity, no hard border), text in dark charcoal (#2D2D2D), medium-weight sans-serif:
Box 1: "ObjectMapper: Java 对象和 JSON 之间的转换器 (ObjectMapper: Converter between Java objects and JSON)"
Box 2: "StringRedisSerializer: 将数据序列化为字符串 (StringRedisSerializer: Serializes data into strings)"
Box 3: "Jackson2JsonRedisSerializer: 使用 Jackson 库序列化为 JSON (Jackson2JsonRedisSerializer: Serializes to JSON using the Jackson library)"
Box 4: "RedisTemplate: Redis 的 Java 客户端 (RedisTemplate: Java client for Redis)"
Box 5: "序列化: 将对象转换为可存储或传输的格式 (Serialization: Converting an object into a format that can be stored or transmitted)"

=== CHIIKAWA STYLE ===
Official Chiikawa aesthetic: round bodies, large sparkly eyes, pink blush marks, tiny limbs, super deformed proportions, gentle line art

=== TEXT RENDERING ===
Typography rules (CRITICAL — apply to every label, title, and annotation):
- Title: floating typography with White Outer Glow or Subtle Drop Shadow — NO solid background band; title integrates naturally into the illustrated scene with decorative Chiikawa elements around it
- All labels placed inside semi-transparent cloud-like blobs (marshmallow-soft, pure white at 70–80% opacity, no hard borders) — soft edges naturally blend into the illustrated background
- Font: bold (600–700) rounded sans-serif for title; medium-weight (500–600) for labels; NO thin, cursive, or brush-stroke fonts
- Text color: dark charcoal (#2D2D2D) on the light blob background; minimum contrast 4.5:1
- Chinese characters: every stroke and radical must be Fully Formed and Vector-style clarity — no hairline breaks, no missing components, no pixelation
- Smooth anti-aliased edges; absolutely NO cracked, fragmented, or glitchy strokes

=== MOOD ===
Informative, friendly, cute but educational

Quality: High-quality Chiikawa illustration, clear visual hierarchy, explaining Redis serialization options
